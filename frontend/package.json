{
  "name": "budgetal",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:3000",
  "dependencies": {
    "antd": "^2.13.6",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "1.0.14",
    "authentication": "file:@budgetal/authentication"
  },
  "scripts": {
    "start": "yarn link-locals && PORT=3001 react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "link-locals": "yarn link authentication"
  }
}
