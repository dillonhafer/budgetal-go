{
  "name": "budgetal",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:3000",
  "eslintConfig": {
    "extends": "react-app"
  },
  "dependencies": {
    "antd": "^2.13.10",
    "classnames": "^2.2.5",
    "papaparse": "^4.3.6",
    "rc-select": "^7.3.1",
    "react-highcharts": "^13.0.0",
    "react-redux": "^5.0.6",
    "react-router-dom": "^4.2.2",
    "react-scripts": "1.0.14",
    "react-transition-group": "^2.2.1",
    "redux": "^3.7.2",
    "ua-parser-js": "^0.7.17"
  },
  "scripts": {
    "start": "source APP_ENV && react-scripts start",
    "build-integration": "NODE_PATH=src:src/utils:src/constants REACT_APP_BASE_URL=http://localhost:3000 react-scripts build && mv build integration-build",
    "test": "source APP_ENV && react-scripts test --env=jsdom",
    "build": "react-scripts build",
    "eject": "react-scripts eject"
  }
}
