# Fix antd 2.0 broken
rm -rf node_modules/antd/node_modules/rc-select && cp -r node_modules/rc-select node_modules/antd/node_modules/

export PORT=3001
export REACT_APP_BASE_URL=http://localhost:3000
export NODE_PATH=$NODE_PATH:src:src/api:src/utils:src/constants:src/routes:src/reducers
export BROWSER="Google Chrome"

if [ -f ".env.local" ]; then
  source .env.local
fi